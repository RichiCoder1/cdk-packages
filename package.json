{
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "turbo run build"
  },
  "turbo": {
    "baseBranch": "origin/main",
    "pipeline": {
      "build": {
        "dependsOn": ["^build"],
        "outputs": [".cdk-out/**", ".jsii"]
      }
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.19.0",
    "turbo": "^1.0.28"
  }
}
